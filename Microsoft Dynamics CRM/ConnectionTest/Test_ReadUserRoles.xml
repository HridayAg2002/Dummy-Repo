<?xml version="1.0" encoding="utf-8"?>
<RestServiceDefinition>
  <Url><![CDATA[/api/data/v9.1/systemusers?$top=1&$expand=systemuserroles_association($select=roleid,name)&$select=systemuserid,domainname]]></Url>
  <Method>GET</Method>
  <ContentType>application/json</ContentType>
  <ResultField>value</ResultField>
  <SuccessCode>200,204</SuccessCode>
  <SqlTable>testUserRoles</SqlTable>
  <SqlExecute>
<![CDATA[
    SELECT
        CASE
            WHEN (SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='testUserRoles') = 1
                 AND (SELECT COUNT(*) FROM testUserRoles) > 0
            THEN 'Successfully able to read user roles'
            ELSE 'Fail to read user roles'
        END AS message,
        CASE
            WHEN (SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='testUserRoles') = 1
                 AND (SELECT COUNT(*) FROM testUserRoles) > 0
            THEN 1
            ELSE 0
        END AS success
]]>
  </SqlExecute>
</RestServiceDefinition>
