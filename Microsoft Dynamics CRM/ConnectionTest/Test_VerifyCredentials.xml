<RestServiceDefinition>
	<Url>https://login.microsoftonline.com/$$settings_Attribute4$$/oauth2/token</Url>
	<Method>POST</Method>
	<ContentType>application/x-www-form-urlencoded</ContentType>
	<SuccessCode>200</SuccessCode>
	<SecurityProtocol>Tls12</SecurityProtocol>
    <DisableAuthenticationFlow>true</DisableAuthenticationFlow>
	<ResultField>access_token</ResultField>
	<FormFields>
		<FormField>
			<Key>client_id</Key>
			<Value>$$settings_Attribute1$$</Value>
		</FormField>
		<FormField>
			<Key>client_secret</Key>
			<Value>$$settings_Attribute3$$</Value>
		</FormField>
		<FormField>
			<Key>resource</Key>
			<Value>$$settings_SapHostName$$</Value>
		</FormField>
		<FormField>
			<Key>grant_type</Key>
			<Value>client_credentials</Value>
		</FormField>
	</FormFields>

    <SqlTable>VerifyCredentialsTable</SqlTable>
    <SqlExecute>
<![CDATA[    
		SELECT
            CASE
                WHEN (SELECT COUNT(*) FROM VerifyCredentialsTable) > 0 
                     AND (SELECT COUNT(*) FROM pragma_table_info('VerifyCredentialsTable') WHERE name = 'access_token') > 0
                THEN 'Successfully able to verify credentials'
                ELSE 'Fail to verify credentials'
            END AS message,
            CASE
                WHEN (SELECT COUNT(*) FROM VerifyCredentialsTable) > 0 
                     AND (SELECT COUNT(*) FROM pragma_table_info('VerifyCredentialsTable') WHERE name = 'access_token') > 0
                THEN 1
                ELSE 0
            END AS success
]]>
    </SqlExecute>
</RestServiceDefinition>
